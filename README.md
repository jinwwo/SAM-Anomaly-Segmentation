# Segment Anything Model for Anomaly Segmentation Evaluation
This project aims to evaluate the performance of the **Segment Anything Model (SAM)** in anomaly segmentation **without additional training**. The goal is to determine whether a foundation model like SAM can effectively segment anomalies.

---

## Table of Contents

1. [Overview](#overview)
2. [How to Use](#how-to-use)
3. [Code Explanation](#code-explanation)

---

## Overview

**1. Dataset**
- The evaluation is conducted using **MVTec-AD Dataset**, which is a benchmark dataset for **industrial anomaly detection**.

**2. Method**
- SAM requires **bounding boxes** or **points** as input prompts.
- However, in anomaly segmentation, we cannot predefine the location of anomalies.
- To test SAM’s **native ability** to perform anomaly segmentation, **ground truth masks** are used to generate prompts for SAM.

**3. Idea**
- Instead of directly using **GT masks**, we propose creating **anomaly maps** that resemble them.
- **Anomaly maps** are generated by comparing the **feature similarity** between **query images** and **normal images**.
- The anomaly map is then **binarized** and used as a **prompt input** for SAM.

## How to Use

1. Prepare the **MVTec-AD dataset**.
2. Run `test_mvtec.py` to evaluate SAM’s anomaly segmentation.
3. Use `anomaly_map.py` to generate **anomaly maps** for SAM prompts.

## Code Explanation

### `test_mvtec.py`
This script creates **SAM's prompt input** using **GT masks** and saves the anomaly segmentation results.
It consists of four different **prompt settings**:

#### a. Naive
- Creates a **box prompt** covering the **entire image**.
```bash
python test_mvtec.py --data_dir "DATASET_DIR"
```

#### b. Box Only (`b`)
- Creates a **bounding box** around the **anomalous region**.
```bash
python test_mvtec.py --data_dir "DATASET_DIR" --mode b
```

#### c. Box with One Point (`bp`)
- Creates a **bounding box** along with **one point** inside the anomalous region.
```bash
python test_mvtec.py --data_dir "DATASET_DIR" --mode bp
```

#### d. Box with Multiple Points (`bps`)
- Creates a **bounding box** along with **multiple points** inside the anomalous region.
```bash
python test_mvtec.py --data_dir "DATASET_DIR" --mode bps
```

### `anomaly_map.py`
This script generates a **heatmap-like anomaly localization map** based on **feature similarity** between a **query image** and **normal images**.

- Uses **Swin Transformer** as the **image encoder** to extract features.
- The generated **anomaly map** is used to create **SAM's prompt input**.
```bash
python test_mvtec.py --data_dir "DATASET_DIR" --save_dir "SAVE_DIR"
```
---
This project explores how **foundation models** like **SAM** can be leveraged for **anomaly detection**, reducing the need for extensive training and annotation.
